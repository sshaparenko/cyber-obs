#cybersecurity #web-security 

Since HTTP is stateless protocol, web applications use libraries, cookies, etc. to keep track of user session

The session identifier (SID) is the unique key that identifies a user session within a database of sessions

Idea is to add some sort of identifier for each request that user sends to the server. 

From this moment, it’s up to server to provide a strong session management mechanism